# Hourly: overwrite grey catalog /assets/nav.js (cash_120 only).
# Empty secrets → exit 0. Does not replace grok / grok-cf.
# ping 2026-09-07T20:00Z-timer: overnight-door timer — Fileman now if RUN-SHIFT002 set CPANEL_TOKEN after the 19:57 6s skip.
# ping 2026-09-07T20:00Z: overnight door — 19:55 schedule missing; last Fileman 19:49 7s skip — Fileman now if RUN-SHIFT002 set CPANEL_TOKEN after the 19:49 skip.
# ping 2026-09-07T19:48Z: last Fileman 19:36 5s skip — Fileman now if RUN-SHIFT002 set CPANEL_TOKEN after the 19:36 skip.
# ping 2026-09-07T19:33Z: 19:25 schedule missing; last Fileman 19:21 8s skip — Fileman now if RUN-SHIFT002 set CPANEL_TOKEN after the 19:18 skip.
# ping 2026-09-07T19:18Z: 19:00–19:20 laptop window closing — Fileman now if RUN-SHIFT002 set CPANEL_TOKEN after the 19:18 skip.
# ping 2026-09-07T19:12Z: 19:00–19:20 laptop window — Fileman now if RUN-SHIFT002 set CPANEL_TOKEN after the 19:04 skip.
# ping 2026-09-07T19:02Z: 19:00–19:20 laptop window — Fileman now if RUN-SHIFT002 set CPANEL_TOKEN after the 19:01 skip.
# ping 2026-09-07T18:51Z: still wait-a-day after 18:46 4s skip — Fileman now if CPANEL_TOKEN was set on GitHub secrets.
# ping 2026-09-07T18:21Z: 18:18 timer after window close — Fileman now if RUN-SHIFT002 set CPANEL_TOKEN after the 18:19 skip.
# ping 2026-09-07T18:18Z: 18:00–18:20 laptop window closing — Fileman now if RUN-SHIFT002 set CPANEL_TOKEN after the 18:14 skip.
# ping 2026-09-07T18:14Z: 18:12 timer — Fileman now if RUN-SHIFT002 set CPANEL_TOKEN after the 18:12 skip.
# ping 2026-09-07T18:11Z: late 18:00–18:20 window — Fileman now if RUN-SHIFT002 set CPANEL_TOKEN after the 18:07 skip.
# ping 2026-09-07T18:05Z: overnight door still wait-a-day — Fileman now if RUN-SHIFT002 set CPANEL_TOKEN after the 18:02 skip.
# ping 2026-09-07T18:01Z: overnight door — Fileman now if CPANEL_TOKEN was set after the 17:59 skip.
# ping 2026-09-07T18:00Z: 18:00 laptop window — Fileman now if CPANEL_TOKEN or WHM aliases were set after 17:52 skip.
# ping 2026-09-07T17:18Z: 17:00–17:20 laptop window closing — Fileman now if token was set after 17:11 skip.
# ping 2026-09-07T17:00Z: 17:00 laptop window — Fileman now if CPANEL_TOKEN or WHM aliases were set after 16:26 skip.
# ping 2026-09-07T16:14Z: late 16:00–16:20 window — Fileman now if CPANEL_TOKEN was set after 16:12 skip.
# ping 2026-09-07T16:12Z: overnight door — Fileman now if CPANEL_TOKEN appeared after 16:04 skip.
# ping 2026-09-07T16:02Z: 16:00–16:20 laptop window — Fileman now if CPANEL_TOKEN was set after 15:28 skip.
# ping 2026-09-07T15:28Z: 15:00–15:20 laptop window closed; Fileman now if CPANEL_TOKEN was set after 15:16 skip.
# ping 2026-09-07T15:15Z: late 15:00 window — Fileman now if CPANEL_TOKEN was set after 15:10 skip.
# ping 2026-09-07T15:10Z: 15:00 window still open; Fileman now if secret was set after 15:03 skip.
# ping 2026-09-07T15:01Z: 15:00 laptop window — Fileman now if CPANEL_TOKEN was just set.
# ping 2026-09-07T14:40Z: still wait-a-day; Fileman now if CPANEL_TOKEN appeared.
# ping 2026-09-07T14:22Z: 14:00-14:20 window closed; Fileman now if CPANEL_TOKEN was armed.
# ping 2026-09-07T14:11Z: 14:00 laptop window — Fileman now if CPANEL_TOKEN was just set.
# ping 2026-09-07T13:29Z: fire now so put-120cash-cron workflow_run can follow.
# ping 2026-09-07T04:56Z: default CPANEL_DIR is live addon docroot
name: put-catalog-nav
on:
  workflow_dispatch:
  schedule:
    - cron: "25 * * * *"
  push:
    branches: ["main"]
    paths:
      - "ftp-drop/nav-night.js"
      - "ftp-drop/write-nav.ps1"
      - "ftp-drop/write-nav-src.ps1"
      - "ftp-drop/patch-nav-src.py"
      - "ftp-drop/fileman-save.sh"
      - ".github/workflows/put-catalog-nav.yml"
jobs:
  put:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Probe catalog nav.js
        id: nav
        run: |
          set -euo pipefail
          night=0
          for host in eidotevil.com agency002.com sebarv.com 120.cash; do
            html="$(curl -fsSL "https://${host}/" || true)"
            js="$(curl -fsSL "https://${host}/assets/nav.js" || true)"
            if echo "$html" | grep -Fq 'ftp-drop/nav-night.js' || echo "$js" | grep -Fq 'cash.keychain.gr'; then
              echo "${host} night nav live"
              night=$((night+1))
            else
              echo "${host} nav.js still year-stamp"
            fi
          done
          if [ "$night" -eq 4 ]; then
            echo "all=yes" >> "$GITHUB_OUTPUT"
          else
            echo "all=no" >> "$GITHUB_OUTPUT"
          fi
      - name: Fileman catalog nav.js
        if: steps.nav.outputs.all != 'yes'
        env:
          CPANEL_HOST: ${{ secrets.CPANEL_HOST }}
          CPANEL_USER: ${{ secrets.CPANEL_USER }}
          CPANEL_TOKEN: ${{ secrets.CPANEL_TOKEN }}
          CPANEL_API_TOKEN: ${{ secrets.CPANEL_API_TOKEN }}
          WHM_API_TOKEN: ${{ secrets.WHM_API_TOKEN }}
          WHM_TOKEN: ${{ secrets.WHM_TOKEN }}
          CPANEL_DIR: ${{ secrets.CPANEL_DIR }}
        run: |
          set -euo pipefail
          chmod +x ftp-drop/fileman-save.sh
          CPANEL_TOKEN="${CPANEL_TOKEN:-${CPANEL_API_TOKEN:-${WHM_API_TOKEN:-${WHM_TOKEN:-}}}}"
          export CPANEL_TOKEN
          if [ -z "${CPANEL_TOKEN:-}" ]; then
            echo "No CPANEL_TOKEN. Skip. Laptop RUN-SHIFT002 still writes nav.js."
            exit 0
          fi
          bytes=$(wc -c < ftp-drop/nav-night.js)
          if [ "$bytes" -le 500 ] || ! grep -Fq 'cash.keychain.gr' ftp-drop/nav-night.js; then
            echo "nav-night.js is not the night pack (bytes=$bytes). Refuse Fileman."
            exit 0
          fi
          USER="${CPANEL_USER:-agency00}"
          DIR="${CPANEL_DIR:-/home/${USER}/domains/120.cash/public_html}"
          for ASSETS in \
            "${DIR}/assets" \
            "/home/${USER}/public_html/120.cash/assets" \
            "/home/${USER}/domains/120.cash/public_html/assets" \
            "/home/${USER}/eidotevil.com/assets" \
            "/home/${USER}/public_html/eidotevil.com/assets" \
            "/home/${USER}/domains/eidotevil.com/public_html/assets" \
            "/home/${USER}/public_html/assets" \
            "/home/${USER}/agency002.com/assets" \
            "/home/${USER}/public_html/agency002.com/assets" \
            "/home/${USER}/sebarv.com/assets" \
            "/home/${USER}/public_html/sebarv.com/assets" \
            "/home/${USER}/domains/sebarv.com/public_html/assets"
          do
            ftp-drop/fileman-save.sh "$ASSETS" nav.js ftp-drop/nav-night.js
          done
      - name: Fileman unique nav.js src
        if: steps.nav.outputs.all != 'yes'
        env:
          CPANEL_HOST: ${{ secrets.CPANEL_HOST }}
          CPANEL_USER: ${{ secrets.CPANEL_USER }}
          CPANEL_TOKEN: ${{ secrets.CPANEL_TOKEN }}
          CPANEL_API_TOKEN: ${{ secrets.CPANEL_API_TOKEN }}
          WHM_API_TOKEN: ${{ secrets.WHM_API_TOKEN }}
          WHM_TOKEN: ${{ secrets.WHM_TOKEN }}
          CPANEL_DIR: ${{ secrets.CPANEL_DIR }}
        run: |
          set -euo pipefail
          chmod +x ftp-drop/fileman-save.sh
          CPANEL_TOKEN="${CPANEL_TOKEN:-${CPANEL_API_TOKEN:-${WHM_API_TOKEN:-${WHM_TOKEN:-}}}}"
          export CPANEL_TOKEN
          if [ -z "${CPANEL_TOKEN:-}" ]; then
            echo "No CPANEL_TOKEN. Skip nav src."
            exit 0
          fi
          USER="${CPANEL_USER:-agency00}"
          DIR="${CPANEL_DIR:-/home/${USER}/domains/120.cash/public_html}"
          patch_nav() {
            local url="$1"
            local guard="$2"
            shift 2
            if ! curl -fsSL "$url" | python3 ftp-drop/patch-nav-src.py "$guard" > /tmp/navsrc.html; then
              echo "skip $url"
              return 0
            fi
            local d
            for d in "$@"; do
              ftp-drop/fileman-save.sh "$d" index.html /tmp/navsrc.html
            done
          }
          patch_nav https://eidotevil.com/ page_100 \
            "/home/${USER}/eidotevil.com" "/home/${USER}/public_html/eidotevil.com" "/home/${USER}/domains/eidotevil.com/public_html"
          patch_nav https://agency002.com/ eidotevil.com \
            "/home/${USER}/public_html" "/home/${USER}/agency002.com" "/home/${USER}/public_html/agency002.com"
          patch_nav https://sebarv.com/ 'pay.html?plan=sitepilot' \
            "/home/${USER}/sebarv.com" "/home/${USER}/public_html/sebarv.com" "/home/${USER}/domains/sebarv.com/public_html"
          # 120.cash index.html is 120-index.html from put-120cash, not a nav-src patch of wait-a-day.
      - name: Verify
        run: |
          sleep 3
          for host in eidotevil.com agency002.com sebarv.com 120.cash; do
            html="$(curl -fsSL "https://${host}/" || true)"
            js="$(curl -fsSL "https://${host}/assets/nav.js" || true)"
            if echo "$html" | grep -Fq 'ftp-drop/nav-night.js' || echo "$js" | grep -Fq 'cash.keychain.gr'; then
              echo "${host} night nav live"
            else
              echo "${host} still year-stamp until CPANEL_TOKEN or laptop RUN-SHIFT002"
            fi
          done
          exit 0
