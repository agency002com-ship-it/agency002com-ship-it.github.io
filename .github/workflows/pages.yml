name: pages
on:
  workflow_dispatch:

# User site agency002com-ship-it.github.io already deploys from the main branch.
# The old push-triggered deploy-pages job failed (~10 min) and did not serve the till.
jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pages ships from the main branch. This workflow is manual only."
