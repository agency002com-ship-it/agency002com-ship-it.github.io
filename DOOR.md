# This homepage is the overnight €120 door

Brief first. Pay on keychain `cash_120`. The return opens `live.html`.
`thanks.html` also reads `?p=` so a 120.cash index can return here after pay.
Do not restore FormSubmit. Do not restore pay-then-wait. Do not paste payment ids.
FTP pack for the indexed 120.cash door: /ftp-drop/
